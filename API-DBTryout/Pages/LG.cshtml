@page
@model LGModel
@{
    ViewData["Title"] = "Home page";
}
<h1>LG</h1>
@foreach (var shop in Model.Shops)
{
    <li>@shop.Name</li>
}

@foreach (var shul in Model.Shuls)
{
    <li>@shul.Name</li>
}